services:
  app:
    image: ussa1976
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ussa1976
    volumes:
      - type: bind
        source: .
        target: /app
    working_dir: /app
    ports:
      - "8889:8888"
    tty: true